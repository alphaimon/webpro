// this is how you comment in 
console.log('Hello Universe 11; has been a while');

let person = {
    name: 'mosh',
    age: 34
};

//   Accessing information from an object
// dot notation, frequently used
person.name = 'Cena';

// Bracket notation, used in UI(user interface)
person[name] = 'Lesnar';
console.log(person.name);
// Cautious about bracket and semiclone

function greet(name, lastname) {
    console.log('Hi ' + name + ' ' + lastname);
}
greet('Shawon', 'Alpha');
//
function square(number) {
    return number*number;
}
console.log(square(45))

